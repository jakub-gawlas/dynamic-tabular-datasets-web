{
  "name": "dynamic-tabular-datasets-web",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://jakub-gawlas.github.io/dynamic-tabular-datasets-web",
  "devDependencies": {
    "custom-react-scripts": "0.0.20",
    "gh-pages": "^0.12.0"
  },
  "dependencies": {
    "mobx": "^2.7.0",
    "mobx-react": "^4.0.3",
    "moment": "^2.17.1",
    "react": "^15.4.1",
    "react-bootstrap": "^0.30.7",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  }
}
